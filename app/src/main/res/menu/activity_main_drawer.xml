<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_constructor"
            app:actionLayout="@navigation/constructor_item_menu" />
        <item
            android:id="@+id/nav_settings"
            app:actionLayout="@navigation/settings_item_menu" />
        <item
            android:id="@+id/nav_lang"
            app:actionLayout="@navigation/lang_item_menu" />
        <item
            android:id="@+id/nav_about"
            app:actionLayout="@navigation/about_item_menu" />
        <item
            android:id="@+id/nav_study"
            app:actionLayout="@navigation/tutorial_item_menu" />
    </group>
</menu>
